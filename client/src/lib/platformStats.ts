// Total analyzed deal volume shown across marketing surfaces. One constant so
// the number can never drift between pages ($2.6B on one page vs $2.68B on
// another reads as hand-maintained). Update here only — ideally replace with
// an API-backed value derived from the analyses table.
export const ANALYZED_VOLUME_LABEL = "$2.68B";
