Fix bug in /auth/google/callback route in routes.ts: When a user logs in with Google but already exists in the users table (they signed up via Deal Analyzer form), you need to ALSO create a user_oauth_accounts record to link their Google account. Currently, the code only checks user_oauth_accounts first but doesn't create the link after finding/creating the user. Add the code to insert into user_oauth_accounts table after user is found/created.
  