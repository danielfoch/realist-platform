node_modules
dist/
coverage/
reports/platform-learning/
.env
.DS_Store
npm-debug.log*
server/public
vite.config.ts.*
*.tar.gz

# Customer data exports — never commit (PII).
# Legitimate CSV fixtures live under data/, server/data/, attached_assets/ and are unaffected.
*_export.csv
/*.csv
.claude/worktrees/
