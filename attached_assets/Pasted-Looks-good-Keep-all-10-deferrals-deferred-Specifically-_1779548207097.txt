Looks good. Keep all §10 deferrals deferred.

Specifically:
- No user-facing refresh button yet
- No per-listing notification delivery yet
- No separate per-listing refresh queue yet
- No scheduler code until this design is reviewed
- No email/Slack/user alert plumbing beyond internal observability yet

Answers to open questions:

1. Scheduler location:
Clyde’s box for v1.

Reason: HomeHarvest/local DB already lives there, Clyde can run the scrape/diff/ingest loop, and Replit should stay as the product/API surface. Don’t put scraping inside the web server.

2. us_listing_sync_runs writes:
Prefer a new authenticated ingest/admin endpoint, not direct DB writes.

Something like:
POST /api/ingest/us-listings/sync-runs
or extend the existing ingest endpoint to accept run metadata.

Reason: prod DB is read-only from Replit tooling and direct writes create split-brain risk. Keep all mutation through authenticated server endpoints.

3. Slack alerting:
Defer. Use Telegram/Clyde report first.

For v1, Clyde sends Dan a daily/failed-run summary:
- markets attempted
- markets succeeded
- markets failed
- listings seen
- price changes detected
- stale markets
- anomalies, especially >10% off-market

If Realist already has an admin webhook later, we can add it. Not needed for implementation kickoff.

HomeHarvest invocation on Clyde side:
Use the existing local stack:
- data/us-listings.db
- scripts/us-listings-scraper.py
- scripts/us-listings-resume.py
- scripts/sync-us-listings-to-realist.py
- scripts/backfill-us-listings-active-flags.py as reference for safe batched ingest

Important implementation constraints:
- batch size should default to 25 unless byte-size batching is implemented
- never reconcile after failed/partial scrape
- one run_id per market scrape
- source of truth is local scrape result → normalized ingest → prod API
- server derives isActive from status, Clyde should not send isActive
- scheduler must work even if us_listing_sync_runs observability table is temporarily unavailable

Next implementation task should be:
Build the v1 Clyde-run market sync runner + Replit ingest/status support needed for sync_runs.

No UI polish. No refresh button. No notification delivery. Just reliable pipeline + observability.