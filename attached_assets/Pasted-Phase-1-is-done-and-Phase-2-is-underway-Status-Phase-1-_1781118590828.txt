Phase 1 is done and Phase 2 is underway. Status:

Phase 1 complete — PR #10: the lineages are reconciled. I merged the live site's 744-commit branch into main, resolving 6 conflict zones (production config wins; Deal Desk routes and their events/share routes coexist; eslint and jest re-pointed at the right tsconfigs). Verified on the merged tree: the idx app is fully green (59/59 tests — theirs plus mine), the frontend builds, and the live app's actual deploy bundle (dist/index.cjs) builds successfully. The live app's 42 pre-existing type errors are byte-for-byte identical on the pure live branch — they ship today via esbuild, which doesn't type-check, so I left them alone.

Confirmed your Sheets mystery while in there: server/googleSheets.ts line 36 fetches the access token from your Replit google-sheet connector for every export — that's exactly why exports land in your account. It gets replaced with the per-user OAuth flow during the port.

Phase 2 started — the port is running now in the background on port/deal-desk-live-app: Deal Desk spine into the live app's conventions (Drizzle tables in shared/schema.ts, a server/dealDesk.ts module mirroring how their events module registers, the two pages adapted into client/, scoring/underwriting modules ported with their tests). It's under a strict gate: no new type errors beyond the pre-existing 42, deploy build must pass, tests must pass. I'll report when it lands.

What you can do meanwhile:

Merge PR #10 on GitHub.
In Replit: Git panel → switch the workspace branch to main → pull. Production behavior doesn't change (same server app, same .replit), but the drift stops and future pulls just work.
Don't merge PRs #4–#8 — they target the dormant app. I'll close each one as its feature gets ported into the live app, keeping the branches as reference.
After the spine port lands, the remaining ports are per-user Sheets OAuth (killing the connector), the pitch deck page into client/, and adding the account gate to their existing UnderwritingSharePage rather than porting mine 