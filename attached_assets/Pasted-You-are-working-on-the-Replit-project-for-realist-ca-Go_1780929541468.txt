You are working on the Replit project for realist.ca.

Goal:
Bring this Replit project fully up to date from the GitHub source of truth, without losing any Replit-only environment variables or secrets.

Important:
- GitHub is the source of truth.
- Do not manually rewrite app files in Replit unless needed to resolve merge/build issues.
- Do not delete secrets, environment variables, database data, or deployment config.
- Before changing anything, inspect the current git state and tell me what branch/remote this Replit project is connected to.

Steps:
1. Run:
   git status
   git remote -v
   git branch --show-current
   git fetch --all --prune

2. Identify the GitHub default branch and latest available branches:
   git branch -a
   git log --oneline --decorate --graph --all -n 20

3. If the working tree has local uncommitted changes:
   - Show me the changed files.
   - Create a safety branch named:
     replit-backup-before-github-sync-YYYYMMDD-HHMM
   - Commit or stash the local changes before pulling.
   - Do not overwrite local changes silently.

4. Sync from GitHub:
   - Prefer pulling the latest intended branch from GitHub.
   - If unsure, use the default branch from origin.
   - Run the safest equivalent of:
     git pull --ff-only origin <branch>
   - If fast-forward is not possible, stop and explain the conflict instead of forcing.

5. Install/update dependencies using the project’s package manager:
   - If pnpm-lock.yaml exists, use pnpm install.
   - Else if package-lock.json exists, use npm install.
   - Else if yarn.lock exists, use yarn install.

6. Run verification:
   - Typecheck if available.
   - Lint if available.
   - Build if available.
   - Run tests if available.
   - Start or restart the Replit app and verify the homepage loads.

7. Report back:
   - Branch synced
   - Latest commit hash
   - Files changed
   - Install command used
   - Build/test results
   - Any conflicts or manual steps needed

Do not force push.
Do not reset hard unless I explicitly approve it.
Do not delete files unless they are clearly generated artifacts or the GitHub branch removes them cleanly through git.