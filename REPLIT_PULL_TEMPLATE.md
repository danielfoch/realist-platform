# REPLIT PULL TODAY

## 1. Date
YYYY-MM-DD

## 2. Branch and commit SHA
- Repo: `https://github.com/danielfoch/realist-platform.git`
- Branch: `BRANCH_NAME`
- Code commit SHA: `COMMIT_SHA`
- Handoff verification: `PASS` / `DO-NOT-DEPLOY`

## 3. What changed
Plain-English summary based only on committed diff.

## 4. Files changed
- `path/to/file`

## 5. Migration steps
`None` or exact migration command/file.

## 6. Env vars needed
`None` or exact variable names. Do not include secrets.

## 7. Replit commands to run
Only include commands verified against `package.json` and existing files.

```bash
npm install
npm run type-check
npm test -- --runTestsByPath test/example.test.ts
npm run build
```

If a command/script/test does not exist, remove it or mark the handoff `DO-NOT-DEPLOY`.

## 8. Test/build result
Generated by:

```bash
python3 scripts/verify-replit-handoff.py --brief REPLIT_PULL_TODAY.md --run-gates
```

Required format:
- Branch exists on origin: PASS/FAIL
- Commit reachable from origin branch: PASS/FAIL
- Listed files exist: PASS/FAIL
- Commands exist in package.json: PASS/FAIL
- Typecheck: PASS/FAIL/NOT RUN
- Targeted tests: PASS/FAIL/NOT RUN
- Build: PASS/FAIL/NOT RUN
- Deploy: No deploy was run

If any required item is FAIL or infrastructure is missing: `DO-NOT-DEPLOY`.

## 9. Risks/blockers
- No deploy was run.
- List real blockers only. Do not invent tests/scripts.

## 10. Plain-English “what Dan should pull into Replit at 10am”
One short paragraph. If verification failed, start with `DO-NOT-DEPLOY` and give the next exact recovery step.
